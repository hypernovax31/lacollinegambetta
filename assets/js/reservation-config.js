/* L'API de réservation est réécrite par Firebase Hosting vers la Cloud
 * Function `reservationApi`. L'URL relative permet de garder le site et
 * Firestore sur le même domaine : aucune clé privée n'est exposée. */
window.LCG_RESERVATION_ENDPOINT = '/api/reservations';
